org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# The Android Gradle Plugin needs a JVM 11+; this machine's default `java` is
# 1.8, and it fails at CONFIGURATION time with an error about resolving the
# classpath rather than about the JDK. Pinned so `gradlew` works from any
# shell without JAVA_HOME set. Change this line if the JDK moves.
#
# MUST BE A FULL JDK, NOT A JETBRAINS JBR. This pointed at CLion's bundled jbr,
# which ships no `jlink` - so Kotlin compiled fine and the build then died in
# :app:compileDebugJavaWithJavac transforming core-for-system-modules.jar. The
# error names the missing exe but reads like an SDK problem, and every task up
# to it succeeds, so it looks like the APK "just stopped building".
org.gradle.java.home=C:/Program Files/BlueJ/jdk
